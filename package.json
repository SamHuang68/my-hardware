{
  "name": "sam-huang-hardware-registry",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm test"
  }
}
